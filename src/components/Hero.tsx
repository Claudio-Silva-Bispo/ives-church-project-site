export default function Hero() {
    return (
        <section id="habilitar-menu">
            <div
                className="relative bg-[#111827]"
                style={{
                    backgroundImage: "linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('/assets/hero/main_image.png')",
                    backgroundSize: 'cover',
                    backgroundPosition: 'center',
                }}
            >
                <div className="container flex flex-col items-start px-4 py-16 pb-20 mx-auto text-start lg:pb-56 md:py-32 md:px-10 lg:px-32 text-gray-50 min-h-[100vh]">
                        
                </div>
            </div>
        </section>
    );
}
